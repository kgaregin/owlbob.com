{% extends ':pages:base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            {% if author_instance == false %}
                {% include ':elements:author_not_found.html.twig' %}
            {% else %}
                {% include ':elements:author_container.html.twig' %}
            {% endif %}
        </div>
    </div>

{% endblock %}